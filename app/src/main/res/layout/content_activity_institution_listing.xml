<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.titomi.workertrackerloginmodule.supervisor.activities.ActivityInstitutionListing"
    tools:showIn="@layout/activity_institution_listing"
    android:layout_margin="5dp"
    android:id="@+id/parent">

    <!--
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView1"
        android:text="Sort by"
        android:textColor="@color/black"
        android:layout_marginTop="10dp"
        />

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:entries="@array/states"
        android:id="@+id/stateSpinner"
        android:layout_toEndOf="@id/textView1"
        android:layout_marginStart="10dp"
        android:layout_marginBottom="20dp"

        />
-->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textAllCaps="true"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:text="Institutions"

        android:layout_marginBottom="10dp"
        android:id="@+id/header"
        />
        <android.support.v4.widget.SwipeRefreshLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/swipeRefresh"
            android:layout_marginTop="20dp">
                <ListView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:id="@+id/institutionList"
                    android:layout_below="@id/header"/>
        </android.support.v4.widget.SwipeRefreshLayout>

</RelativeLayout>
